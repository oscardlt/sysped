             PGM
             DCL        VAR(&TIME) TYPE(*CHAR) LEN(6)
             CALL       PGM(SYSPED/INLIBL)

 START:      CALL       PGM(EUR03AR) /* ALLE TYPER                     */

   /* VENT XX SEK FØR NESTE SYKLUS */
             DLYJOB     DLY(30)
             RTVSYSVAL  SYSVAL(QTIME) RTNVAR(&TIME)
             IF         COND(&TIME *GT '230000') THEN(GOTO CMDLBL(END))
             GOTO       CMDLBL(START)

 END:        ENDPGM
