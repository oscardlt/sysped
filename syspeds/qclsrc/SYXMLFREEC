             PGM
  /* Tempfil for å legge ut data i.. */
             DLTF       FILE(QTEMP/Q)
             MONMSG     MSGID(CPF0000)
             CRTPF      FILE(QTEMP/Q) RCDLEN(1024)

  /*Skap en mini demo xml i qtemp/q */
             CALL       PGM(SYXMLFREE)

  /*Kopier til STMF for lettere å se på den (i f.eks IE explorer) */
             CPYTOSTMF  FROMMBR("/QSYS.LIB/QTEMP.LIB/Q.FILE/Q.MBR") +
                          TOSTMF("/SYSPEDDEV/TEMP/XMLTEST.XML") +
                          STMFOPT(*REPLACE) CVTDTA(*AUTO) +
                          STMFCODPAG(*PCASCII) ENDLINFMT(*CRLF)

             ENDPGM
