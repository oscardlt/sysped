     c                   move      *blanks       result           27
     C                   EVAL      result = 'DETTE ER "UTROLIG" MEN SANT'
      * // %REPLACE with 4 parameters to insert text:
      * //result = %replace ('/"': result: %scan ('"': result): 0);
      * //DETTE ER /""UTROLIG" MEN SANT
     c                   z-add     1             pos               5 0
     c                   dow       pos <> 0
     c                   EVAL      pos  = %scan ('"' : Result : pos)
     c                   if        pos <> 0
     c                   eval      result = %replace ('/"': result: Pos :1)
     c                   eval      pos=pos+2
     c                   endif
     c                   enddo

     C                   SETON                                        LR
     C                   RETURN
