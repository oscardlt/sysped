/* SENDE FIL TIL SEG SELV      */
/* CPYCMD ENDRET               */
             PGM        PARM(&FILE &MBR)
             DCL        VAR(&FILE) TYPE(*CHAR) LEN(10)
             DCL        VAR(&MBR) TYPE(*CHAR) LEN(10)
             DCL        VAR(&FUT) TYPE(*CHAR) LEN(10) VALUE(TMPEXC)
             DCL        VAR(&MUT) TYPE(*CHAR) LEN(10) VALUE(TMPEXC)
             DCL        VAR(&MBR) TYPE(*CHAR) LEN(10)
             DLTF       FILE(QTEMP/TMPEXC)
             MONMSG     MSGID(CPF0000)
             RUNQRY     QRY(*NONE) QRYFILE((*LIBL/&FILE)) +
                          OUTTYPE(*OUTFILE) RCDSLT(*YES) +
                          OUTFILE(QTEMP/&FUT)
             CALL       PGM(SYGE55CL) PARM(&FUT &MUT)
 SLUTT:      ENDPGM
