             PGM
             /*_____________________*/
             /* Deklarere Variabler */
             /*"""""""""""""""""""""*/
             DCL        VAR(&LIB) TYPE(*CHAR) LEN(10)
             DCL        VAR(&LSLIB) TYPE(*CHAR) LEN(10) +
                          VALUE('SYSPEDTX  ')
             DCL        VAR(&W0035) TYPE(*CHAR) LEN(1)
             DCL        VAR(&RMTSYS) TYPE(*CHAR) LEN(64) +
                          VALUE('distr.tullverket.se')
             /*____________________________________*/
             /* Hvilket bibliotek ligger filen i ? */
             /*""""""""""""""""""""""""""""""""""""*/
             RTVDTAARA  DTAARA(EDIDTA (171 10)) RTNVAR(&LIB)
             /*__________________________________*/
             /* Lag CMD fil for å hente oversikt */
             /*""""""""""""""""""""""""""""""""""*/
             CLRPFM     FILE(SVT203FR)
             MONMSG     MSGID(CPF0000)
             CLRPFM     FILE(SVT203FS)
             MONMSG     MSGID(CPF0000)
             CALL       PGM(SVT203R1)
             /*_______________*/
             /* Hent oversikt */
             /*"""""""""""""""*/
             ADDLIBLE   LIB(&LSLIB) POSITION(*LAST)
             MONMSG     MSGID(CPF0000)
             CHGCURLIB  CURLIB(&LSLIB)
             OVRDBF     FILE(INPUT) TOFILE(SVT203FR)
             OVRDBF     FILE(OUTPUT) TOFILE(SVT203FS)
             STRTCPFTP  RMTSYS(&RMTSYS)
             CHGCURLIB  CURLIB(*CRTDFT)
             CHKOBJ     OBJ(&LSLIB/LSOUTPUT) OBJTYPE(*FILE) +
                          MBR(LSOUTPUT) AUT(*ALL)
             MONMSG     MSGID(CPF0000) EXEC(GOTO CMDLBL(EXIT))
             OVRDBF     FILE(LSOUTPUT) TOFILE(&LSLIB/LSOUTPUT)
             /*_______________________________*/
             /* Lag CMD fil for å Hente filer */
             /*"""""""""""""""""""""""""""""""*/
             CLRPFM     FILE(SVT203FR)
             MONMSG     MSGID(CPF0000)
             CLRPFM     FILE(SVT203FS)
             MONMSG     MSGID(CPF0000)
             CALL       PGM(SVT203R2) PARM(&LIB)
             /*__________________*/
             /* Hent komponenter */
             /*""""""""""""""""""*/
             OVRDBF     FILE(INPUT) TOFILE(SVT203FR)
             OVRDBF     FILE(OUTPUT) TOFILE(SVT203FS)
             STRTCPFTP  RMTSYS(&RMTSYS)
             MONMSG     MSGID(CPF0000)
 EXIT:
             ENDPGM
