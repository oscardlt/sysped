             PGM

             DLTF       FILE(QTEMP/QSHLS)
             MONMSG     MSGID(CPF0000)
             CRTPF      FILE(QTEMP/QSHLS) RCDLEN(1000)
             OVRDBF     FILE(STDOUT) TOFILE(QTEMP/QSHLS) +
                          OVRSCOPE(*JOB)

/*           QSH        CMD('ls -R -ls /home/Jovo')  */
             QSH        CMD('ls -R /home/Jovo')

             DLTOVR     FILE(STDOUT) LVL(*JOB)
             SNDPGMMSG  MSG('Fil QTEMP/QSHLS inneholder output. +
                          (Se tidsbruk: dspjoblog ..)')

             ENDPGM
