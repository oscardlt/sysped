             PGM        PARM(&USE &TXT)
             DCL        VAR(&USE) TYPE(*CHAR) LEN(10)
             DCL        VAR(&TXT) TYPE(*CHAR) LEN(50)
             RTVJOBA    USER(&USE)
             RTVUSRPRF  USRPRF(&USE) TEXT(&TXT)
             ENDPGM
